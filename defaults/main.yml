---
# Track the latest Active LTS
# See https://nodejs.org/en/about/releases/
nodejs_version: 14
# Enable the source repo
nodejs_src: false
# Install additional packages
nodejs_packages:
  - sass
...
